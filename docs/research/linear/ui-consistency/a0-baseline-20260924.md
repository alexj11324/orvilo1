# UI consistency remediation baseline — 2026-09-24

The source baseline for this audit is `460fe703abe264d436c90b579bed0bfe6523e1b5`
on `fix/linear-parity-issue-detail-layout` (draft PR #227). That branch contains
PR #226 at `a6005a036` and PR #225 at `78502cbbf`. All three PRs were open when
rechecked. The main checkout at `fix/loading-screen-brand-mark` has unrelated
untracked files; this work uses `/private/tmp/orvilo-ui-related-a1` and leaves
that checkout intact.

This table rechecks the 20 records in the supplied audit against that one
source revision. "Present" is a source finding, not a claim that every control
was clicked in the product. Conditional records remain pending until the
specified runtime or business decision is available.

| ID  | Current disposition at `460fe703` | Evidence boundary / next action                                                                                        |
| --- | --------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| F01 | Present                           | `TaskStatusTag` can display a workflow glyph while its menu writes execution status.                                   |
| F02 | Present                           | Detail Properties shows a static workflow badge and a separate editable execution row.                                 |
| F03 | Present                           | `TaskWorkflowBadge` requires a provider state ID before showing workflow visuals.                                      |
| F04 | Present                           | Board grouping in `TaskModel` selects workflow or execution using provider ID presence.                                |
| F05 | Present                           | Badge name and color derive from category, not the concrete team state.                                                |
| F06 | Changed shape, incomplete         | Parent row tries workflow glyph first but can fall back to execution; loading/unknown handling remains.                |
| F07 | Changed shape, incomplete         | Subtask popup tries workflow glyph first but retains execution fallback and local status mapping.                      |
| F08 | Changed shape, incomplete         | Triage DTO now carries workflow fields and row tries workflow glyph first; missing provider ID falls back.             |
| F09 | Present at baseline               | Related form creates `blocks`; A1 changes this to `relates` and protects existing blockers.                            |
| F10 | Changed shape, incomplete         | Related rows try workflow glyph first but still require provider ID.                                                   |
| F11 | Present                           | Project Issues Status filter and generic context menu use execution values.                                            |
| F12 | Present                           | Project status filter catalog omits `planned`.                                                                         |
| F13 | Present in this section           | Project detail rail has navigation for a present project and no add/change field here. Other entry points not audited. |
| F14 | Pending measurement               | Typography roles differ across surfaces; no current-revision cross-page computed-style matrix yet.                     |
| F15 | Present                           | Existing lint restrictions do not cover every status consumer or mixed-axis behavior.                                  |
| F16 | Type risk, no live failure shown  | Optional `TaskStatusTag` action target remains; inspected editable callers pass one.                                   |
| F17 | Conditional risk                  | Triage missing `domainRevision` can stop a mutation; real DTO absence not reproduced.                                  |
| F18 | Conditional decision              | Running-task Assignee/Labels are disabled; permission and safety reason need runtime review.                           |
| F19 | Behavior known, semantics pending | Triage update writes triage fields without an explicit workflow change.                                                |
| F20 | Metric definition pending         | Subtask completion count uses execution `completed`; product meaning needs a decision.                                 |

## Current acceptance scope

The first implementation slice is A1: a normal Related entry must create a
nonblocking relation, appear from both readable issues, avoid duplicate
reverse edges, preserve existing `blocks`, and remove the relation from either
side without removing a blocker. Tests cover the domain and router; Web runtime
must use ordinary clicks and independent readback on the local parity database.
Existing historical `blocks` edges are not migrated by this slice.

The remaining work follows the supplied A2–A7 sequence. The route and control
inventory must cover My Issues, Team Issues list/board, Project Issues
list/board, full issue detail and peek, parent/subtask/Related, Team Home and
Triage, Project Overview, Saved Views, Inbox detail, search, and shared
property menus. Those surfaces remain pending unless a later revision records
an action, readback, platform, and exact source SHA for each control.
