# Linear parity — issue main column

Two-way alignment of the issue detail main column (breadcrumb → title →
description → sub-issues → activity) against the live Linear product, per
`.agents/skills/linear-ui-parity`. Reference: `bdiverifier` workspace, ORV
team issues; candidate: seeded parity fixture (`scripts/seedLinearParity`).

| #   | Probe finding                                                                     | Verdict / action                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M4  | Breadcrumb rendered 3 levels (`project > parent > issue`) vs Linear's 2           | **Aligned** — ancestor crumbs removed; the parent is already reachable via the "Sub-issue of" row (`TaskParentBar`), exactly like Linear.                                                                                                                                                                                                                                                                                             |
| M5  | Sub-issues section named "Subtasks", no trailing add row                          | **Aligned** — copy was renamed to "Sub-issues"/"Add sub-issue" in #249; this PR adds Linear's always-visible "Add sub-issue" row under the list and moves the inline composer to the end of the list (was above it). Header `Run all`/`＋` icons already carry `aria-label` (#247); `Run all` is kept — agent-domain feature. Row chips (priority, automation, agent avatar) kept — agent-domain extras the user directed us to keep. |
| M8  | Timestamps absolute (`Sep 15`) vs Linear compact relative (`4d ago`)              | **Aligned** — `useActivityTime` gains a `compact` mode (`formatRelative` injection on `formatActivityTime`); comment/topic/activity timestamps on the issue page now render `just now`/`Xm/Xh/Xd/Xw/Xmo/Xy ago` with the full datetime still on hover. Other feeds keep the 24h-then-date convention.                                                                                                                                 |
| M10 | Comment placeholder carried the agent suffix "— comments also guide the next run" | **Aligned** — placeholder trimmed to Linear's `Leave a comment...` (en + zh).                                                                                                                                                                                                                                                                                                                                                         |
| M11 | App font Geist vs Linear Inter Variable                                           | **Aligned** — `--font-sans` now loads `Inter` (variable); `genFontFamily` head family `Geist` → `Inter`; code stack head `Geist Mono` → `Berkeley Mono` (Linear's code font; falls through to `ui-monospace` when not installed).                                                                                                                                                                                                     |
| M12 | Description body 16px/400 vs Linear 15px/450                                      | **Aligned** — `TaskInstruction` passes `contentStyle { fontSize: 15, fontWeight: 450 }` to the editor canvas. Sub-issue row text already at 13px.                                                                                                                                                                                                                                                                                     |
| M3  | No "Add reaction" / formal "Attach" row under the description                     | **Follow-up** — needs a task-level reactions backend (reactions exist only on acceptance comments) and a first-class attachments row; tracked separately.                                                                                                                                                                                                                                                                             |
| M7  | No "Unsubscribe" / "Change subscribers" affordance                                | **Follow-up** — task subscriptions do not exist yet (workspace-level only); tracked separately.                                                                                                                                                                                                                                                                                                                                       |
| M9  | No comment ⋯ options menu or relation history rows ("marked blocked by")          | **Partially here** — comment doc cards + ⋯ options verified in a follow-up; relation activity rows need dependency writes to emit `taskActivities` rows (currently only domain events `task.dependency.changed`), tracked separately.                                                                                                                                                                                                 |

Follow-up scope: M3 (reactions + attach row), M7 (subscriptions), M9
(relation history rows + comment options) ship in the next parity PR — each
needs server-side support, not just chrome.

## Evidence

Scoped `bun run check --lint --test` on all touched files: 61 tests passed.
Runtime verification screenshots attached to the PR (compact timestamps,
2-level breadcrumb, trailing "Add sub-issue" row, Inter font, 15px/450
description).
