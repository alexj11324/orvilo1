# Project Issues row order and tabs — Electron acceptance, 2026-09-24

Product source commit: `195b98864` on branch `fix/linear-parity-project-detail`. The Electron captures were made on the same code before the formatting-only commit hook ran. The live Linear measurements and frozen criteria are in `docs/research/linear/project-detail/audit-2026-09-24.md`.

The candidate ran as a compiled Electron dev app from this worktree, with its own Vite `:5197`, CDP `:9251`, and copied, isolated user data at `/tmp/orvilo-project-detail-ud-9251`. The renderer URL was `app://renderer/ws-useragenttes/project/parity-voyager-launch/tasks`; viewport override was 1440 × 900 CSS pixels at DPR 2, zh-CN, dark appearance. `probe-rows.js` was evaluated through `.agents/acceptance/scripts/cdp-inspect.cjs`; its result is `electron-project-rows-1440.json`.

## Observed result

- The populated project list rendered five visible rows. All five have direct title-row children in `[priority icon][ID][status icon][title]` order; each status child is at index 2. One row has an additional sub-issue marker after the title. See `electron-project-issues-1440.png` and the JSON probe.
- Adjacent rows within the same status group start 44 px apart. Group boundaries add the group header, so the complete top-to-top differences are 88, 44, 44, and 90 px. Individual row boxes measure 42–44 px. No workflow text chip rendered on this project Issues page.
- The project header rendered only `概览 | 动态 | 问题` (Overview | Activity | Issues). Direct navigation to `/project/parity-voyager-launch/milestones` still rendered its milestone page and two populated milestones; it did not restore a Milestones tab.
- Linked Project Issues now show the same workflow category glyph as the board header and workflow badge, from `WORKFLOW_CATEGORY_VISUALS`. The live fixture has `in_review` (clock), `backlog` (dashed circle), `todo` (circle), and `done` (check circle) across five rows. None exposes an execution-status menu trigger; see `electron-workflow-icons-1440.json` from `probe-workflow-icons.js`. The earlier execution-status-menu capture is kept as `before-workflow-glyph-status-menu-1440.png` to document the corrected mismatch.
- A real CDP `Input.dispatchMouseEvent` click on VYG-6's workflow glyph navigated to `/task/VYG-6/voyager-backlog-post-launch-retro-outline`; no menu opened. This is a display-only glyph inside the clickable row, matching the reference interaction. Unlinked tasks keep the `TaskStatusTag` execution fallback, covered by the focused component regression. A real click on VYG-6's priority icon opened None/Urgent/High/Medium/Low (`electron-priority-menu-1440.png`). No option was selected, so the fixture was not changed.
- My Issues retained `[priority][status][workflow chip][ID][title]`; the populated page had 19 workflow chips (`electron-my-issues-1440.png`). Team Issues remained a populated board with 50 workflow chips (`electron-team-issues-1440.png`). These two routes do not receive the Project Issues row flag.

`bun run check` with the five changed TSX paths passed after the workflow glyph fix: lint clean after one formatting autofix, 86 related tests passed. The new counterexample uses a linked `in_review` workflow and `scheduled` execution status; it asserts the workflow glyph renders and the execution glyph does not. No local `tsgo` ran. Remote CI remains a separate gate for the pushed head. The evidence covers the frozen 1440 × 900 row and tab slice, not the other whole-page gaps listed in the audit.
