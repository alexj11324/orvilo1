# Reviews 页只读审计 — 2026-09-23

- **对象**：ORV-139 Reviews 页（`/reviews`），对照 Linear `/bdiverifier/reviews`（+ `/reviews/created`、`/review/:slug`）。
- **基线**：工作树 HEAD `5358ec676`（工作区干净，唯一未跟踪文件是 `docs/research/linear/PARITY-MATRIX.md`）。上一轮实现落在 `c191c163e`（master-detail shell + stale 隔离），运行时证据见 `CANDIDATE-VERIFICATION.md`（2026-09-22，Electron/CDP，断连账号）。
- **方法**：纯只读源码 + 既有证据复核。未跑 test/lint/dev server，未碰 CDP。
- **重要前提**：`cand-sweep.md` §3 与 TRIAGE R2/R3 引用的 `ReviewsPage.tsx:272/273/323` 行号是 **c191c163e 之前的单栏旧版**；当前文件已重写为双栏（`listPane` 在 :303-413）。本审计一律按当前源码重新判定。

## 1. 差异清单（当前源码 vs 参考端实测）

| #   | 面               | 参考端（`/bdiverifier/reviews`）                                                                                                                  | 候选端（当前 HEAD）                                                                                                                                                | 位置                                                                                                                                                                                    | 判定                                                                                                                                                                           |
| --- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D1  | 分段文案         | `For you`（12px/500，实测）                                                                                                                       | `For me`（en-US）/ `待我审核`（zh-CN）                                                                                                                             | `packages/locales/src/default/common.ts:657` `myWork.reviewsForMe`；`locales/en-US/common.json:469`、`locales/zh-CN/common.json:469`；消费于 `src/features/Reviews/ReviewsPage.tsx:291` | **仍开放（R1）**—— 最廉价确定项，locale owner 不在本 lane                                                                                                                      |
| D2  | Created 分段 URL | `/reviews/created` 路径段                                                                                                                         | `/reviews?tab=created` 查询参数                                                                                                                                    | `reviewsSurface.ts:4-5`                                                                                                                                                                 | URL 形态差，语义等价                                                                                                                                                           |
| D3  | 详情路由         | `/review/:slug`（单数）；Diff 为 `/review/:slug/changes`                                                                                          | `/reviews/:reviewId`（复数）；无独立 changes 路由                                                                                                                  | `reviewsSurface.ts:10-11`；`desktopRouter.shared.tsx:773`                                                                                                                               | URL 形态差；Diff tab 属 D10                                                                                                                                                    |
| D4  | 页头右侧控件     | `Add filter`（221×508 菜单）+ `Display options`（锚定面板），清单见 `BEHAVIORS.md` §Header menus                                                  | 无 ——`NavHeader` 只传 `left` 标题                                                                                                                                  | `ReviewsPage.tsx:306-312`                                                                                                                                                               | **有意不做**：queue 契约仅 `tab`+`cursor`，spec §Candidate behavior 7 明确不渲染空菜单                                                                                         |
| D5  | For-you 分组     | `Ready to merge` + `Created by you` 两组（12/500、sticky、可折叠、**不带计数**）                                                                  | 单个 `Pull requests` 组头（sticky、不带计数、纯 `Text` **不可折叠**）                                                                                              | `ReviewsPage.tsx:329-331`、`styles.queueHeading` :62-72                                                                                                                                 | **已知缺口**：queue 响应无分组字段（spec §Remaining gaps）；且候选端 `for-me` 只含「请求我审」的 PR，没有「我发起且 ready」这一组的数据来源                                    |
| D6  | Created 组名     | `Open`                                                                                                                                            | `Open`（`reviews.state.open`）                                                                                                                                     | `ReviewsPage.tsx:330`、`common.ts:751`                                                                                                                                                  | ✅ 文案一致                                                                                                                                                                    |
| D7  | 额外区块         | 无对应物                                                                                                                                          | `In-product approvals` 第二节，内嵌 `WorkQueryResults`（其状态组头**带计数**且可折叠）                                                                             | `ReviewsPage.tsx:384-409`；组头计数 `WorkQueryResults.tsx:282-284`                                                                                                                      | 候选端产品多出块，非参考端形态；其组内计数与参考端「组头不带计数」风格相反                                                                                                     |
| D8  | 行内容           | PR glyph + 标题 13/500 + 条件状态 glyph + 相对时间 13/450 右对齐 + 草稿笔图标                                                                     | PR glyph (14px,success 色) + 标题 13/500 + 决议 `Tag`（Approved 绿 / Changes requested 红）+ 相对时间 12px                                                         | `PullRequestRow` `ReviewsPage.tsx:146-189`                                                                                                                                              | 近似对齐：`Tag` 取代了 glyph 槽位；`QueueItem.isDraft` 已取数但**未渲染**草稿指示（小缺口）                                                                                    |
| D9  | 详情空态         | 插画 143×152 居中 + `45 reviews` 一行 13/500                                                                                                      | `GitPullRequestIcon` 44px + `{total}` 数字 + `Pull requests` 两行                                                                                                  | `ReviewsPage.tsx:428-436`                                                                                                                                                               | 近似（图标代插画、计数与标签拆两行）；语义同为「聚合计数占位」                                                                                                                 |
| D10 | 详情 IA          | `Overview`/`Diff` 双 tab；PR 操作菜单（merge/close/draft/link issue/select reviewers/copy 子菜单）+ 仓库身份菜单；`BEHAVIORS.md` §Selected detail | 嵌入 `ReviewPullRequestPage`：240px 文件导航 + 状态 / 分支 /+−/checks 摘要 + body + checks 面板 + diff 卡 + conversation + 底部 submit 面板；只有 `Open in GitHub` | `ReviewPullRequestPage.tsx:480-797`                                                                                                                                                     | **有界非目标**（PLAN §Bounded non-goals）：候选端是可写评审面，不做 Linear IA 复制                                                                                             |
| D11 | 列表分页         | 未观察到 load-more / 计数（21 行全渲染）                                                                                                          | `Showing X of Y` + `Load more`                                                                                                                                     | `ReviewsPage.tsx:358-379`                                                                                                                                                               | 候选端多出（契约驱动的合理 affordance，非缺陷）                                                                                                                                |
| D12 | 窄屏             | 整宽列表；选中详情为前台面；返回恢复列表上下文                                                                                                    | 同语义：detail overlay z3 + `返回拉取请求列表`，列表保持挂载                                                                                                       | `ReviewsPage.tsx:44-52,449-453`；`reviewsSurface.ts:21-24`                                                                                                                              | ✅ 已对齐，**运行时验证过**（768px，含 z-index 遮挡修复）                                                                                                                      |
| D13 | 宽屏分栏         | 482 / 705（列表 / 详情）                                                                                                                          | `listWidth={482}` + detail `flex:1`                                                                                                                                | `ReviewsPage.tsx:447`                                                                                                                                                                   | ✅ 已对齐且实测（482/704 @1440）。**细注**：`WorkSurface.tsx:110` `splitList` 带 `max-width: 45%`，容器 <\~1071px 时列表被压到 45% 而非 482——lg 断点附近的中间态需确认是否有意 |
| D14 | 侧栏角标         | `18`（口径疑似「待我处理」）                                                                                                                      | `待审核` 项带角标                                                                                                                                                  | 侧栏 lane，矩阵 A 节标 `impl`                                                                                                                                                           | impl，计数口径未逐项核对                                                                                                                                                       |
| D15 | 详情标题栏       | PR 标题 + additions/deletions 在头部 + 操作菜单                                                                                                   | 返回箭头（宽屏嵌入时隐藏）+ PR 标题；状态 / 分支 /+− 移至内容区                                                                                                    | `ReviewPullRequestPage.tsx:482-499,586-604`                                                                                                                                             | 近似，IA 归属 D10                                                                                                                                                              |
| D16 | 双空态重复       | —（参考端无此区块）                                                                                                                               | **已消除**：`externalReviews.length > 0 ? … : undefined` 使空数组不再渲染 `reviewBlock`                                                                            | `ReviewsPage.tsx:397` vs `WorkQueryResults.tsx:389-404`                                                                                                                                 | R2 已在源码层修复（c191c163e 同批），运行时待复核                                                                                                                              |

## 2. R1–R6 现状判定

| #   | TRIAGE 原判定                                             | 现状                                       | 依据                                                                                                                                                                                                                                                                                           |
| --- | --------------------------------------------------------- | ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | **真实缺陷**：`For you` vs `For me`                       | **仍开放**                                 | `common.ts:657` 仍为 `'For me'`（行号自 :650 漂移 +7；`en-US common.json:469`、zh-CN `待我审核` :469 同值）；`ReviewsPage.tsx:291` 仍消费 `myWork.reviewsForMe`。`c191c163e` 未动 locale（PLAN 声明 locale 不在本 lane）                                                                       |
| R2  | `In-product approvals` 下同一句空态连渲两次               | **已在源码层修复**                         | `ReviewsPage.tsx:397` 改为 `externalReviews.length > 0 ? externalReviews : undefined`；空数组 → `reviewBlock=null`（`WorkQueryResults.tsx:389`），只剩底部单个 `Empty`（:449/:467）。运行时未复核（断连账号下该节是否仍单空态，见 §4）                                                         |
| R3  | 单栏 + 两堆叠区块 vs 双栏 master-detail                   | **已修（含运行时证据）**                   | `WorkSurfaceSplit` 482/flex（`ReviewsPage.tsx:442-448`）；`CANDIDATE-VERIFICATION.md` 实测 `[245,47,482,824]` + `[727,47,704,824]`；窄屏 overlay/Back 亦实测通过                                                                                                                               |
| R4  | 参考端行是 **PR 不是 issue**                              | **已对齐（源码层）**                       | 列表数据即 `pullRequestService.queue`（`ReviewsPage.tsx:210-216`），行组件 `PullRequestRow` 带 `GitPullRequestIcon`（:168）。⚠️ 填充行、`data-active` 选中填充未在运行时见过（账号未连 GitHub）                                                                                                |
| R5  | 分组头缺失（`Ready to merge`/`Created by you`，不带计数） | **部分对齐 + 显式 known-gap**              | 候选端已有 sticky 组头且**不带计数**（`Pull requests`/`Open`/`In-product approvals`）——「不带计数」这条与参考端一致；但 for-me 下无 `Ready to merge`/`Created by you` 两组、组头不可折叠（纯 Text，非 button+disclosure）。归因为服务契约缺口（spec §Remaining gaps、BEHAVIORS §Group header） |
| R6  | 三处空态三种规格                                          | **维持「非左连接差异」判定；形态仍有多种** | 当前空态：列表断连 `Empty+CTA`（:334-340）、队列空 `Empty`（:343-345）、in-product 空（`WorkQueryResults` :449/:467）、详情占位 icon + 计数（:428-436）、详情断连 `Empty` 无 CTA（:419-422）。TRIAGE §5.4 判 UNRESOLVED—— 参考端同位置无对应物，不算 parity 缺陷；R2 的双重渲染已消除          |

**净结论**：R3 已闭环（运行时级）；R4 源码级闭环；R2 源码级修复待运行时复核；R5 部分对齐、余量为契约缺口；R1 是唯一仍开放的廉价确定缺陷；R6 非差异项维持原判。

## 3. 状态覆盖清单（候选端 × 证据层级）

| 状态                 | 实现                                                         | 位置                                                                  | 证据层级                                                                    |
| -------------------- | ------------------------------------------------------------ | --------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| 宽屏默认（无选中）   | split 482 + 详情聚合占位                                     | `ReviewsPage.tsx:415-437,442-448`                                     | ✅ 运行时（断连态下 482/704）                                               |
| 宽屏选中             | split + 嵌入 `ReviewPullRequestPage`（`embedded` 隐藏 Back） | :415-416                                                              | ✅ 运行时（断连态）；填充态仅源码                                           |
| 窄屏列表             | 整宽列表（`splitListSolo`）                                  | `reviewsSurface` `'list'`；`WorkSurface.tsx:112-116`                  | ✅ 运行时（768px）                                                          |
| 窄屏详情             | overlay z3 + Back→`listPath`                                 | `ReviewsPage.tsx:44-52,449-453`                                       | ✅ 运行时（含遮挡修复复测）                                                 |
| 队列加载中           | `SkeletonList`                                               | :332-333                                                              | 源码                                                                        |
| GitHub 断连          | 列表 `Empty`+Connect CTA；详情 `Empty`（**无 CTA**）         | :334-340；:419-422                                                    | ✅ 运行时（列表 + 详情文案）；详情无 CTA 为小不对称                         |
| 队列错误             | `AsyncError` block + retry                                   | :341-343；:423-426                                                    | 源码                                                                        |
| 队列空               | `reviews.queueEmpty` `Empty`                                 | :343-345                                                              | 源码                                                                        |
| 队列填充             | `PullRequestRow` + `data-active` 填充                        | :346-357；:91-93                                                      | 源码（运行时未见过）                                                        |
| 队列分页             | `Showing X of Y` + `Load more`（tail 追加、游标推进）        | :218-251,:358-379                                                     | 源码（pager 层有 `reviewPager.test.ts`，queue tail 无专项测试）             |
| In-product approvals | `WorkQueryResults` list 布局（组头带计数、可折叠、组内分页） | :384-409                                                              | 源码                                                                        |
| 详情四态             | loading / 断连 + CTA /error/ 填充                            | `ReviewPullRequestPage.tsx:561-794`                                   | 断连 ✅ 运行时；其余源码                                                    |
| stale / write gate   | `staleBanner` + 按 key 隔离的 `reviewStaleState`             | `reviewStaleState.ts`；`ReviewPullRequestPage.tsx:577-584`            | ✅ 单测（A/B 交错回归，CANDIDATE-VERIFICATION §Read-only review follow-up） |
| 路由保活             | `reviews/:reviewId?` 单路由身份，切详情不 remount            | `desktopRouter.shared.tsx:766-774`；`mobileRouter.config.tsx:216-221` | ✅ 单测（`reviewsRoutePersistence.test.ts`、`reviewsSurface.test.ts`）      |

## 4. 待运行时验证项（需 GitHub 已连接账号）

1. **填充队列**：行渲染（PR 图标 / 决议 Tag / 相对时间）、`data-active` 选中填充、行 Enter / 点击 → `/reviews/:reviewId` 且列表保持挂载、返回恢复 `listPath`+ 滚动位。
2. **R2 复核**：`In-product approvals` 空态只渲染一次（源码已修，断连 / 空数据两种账号状态下各看一次）。
3. **分页实击**：`Showing X of Y` 与 `Load more` 的 tail 追加与游标推进；组内 `loadMoreGroup`。
4. **详情填充态**：stale banner 出现 / 解除、submit 面板 write gate（含 A/B 交错 stale 不回开）。
5. **分组头 sticky 实滑**：`queueHeading`（top 88px）与 `listChrome`（top 0, z2）叠压次序；`In-product approvals` 内 `WorkQueryStatusGroup` 组头的计数显示确认（与参考端「组头不带计数」相反，定位在多出区块内）。
6. **45% cap 中间态**：容器宽介于 lg 断点与～1071px 之间时列表实为 45% 而非 482px（`WorkSurface.tsx:110`）—— 确认是否有意为之。
7. **R1 修复路径**（若决定修）：改 `myWork.reviewsForMe` 值还是新 key，属 locale owner；zh-CN 对应文案需同步定（参考端中文文案未采集，`For you`→`给你`/`待你处理` 类需裁决）。

## 5. 引用

- 参考端：`ref-sweep.md` §3、`TRIAGE.md` §0.1（1-3 条）/§3.4/§5.4、`BEHAVIORS.md`、`PAGE_TOPOLOGY.md`
- 规格：`components/master-detail-shell.spec.md`（几何值 + 有界缺口清单）
- 运行时证据：`CANDIDATE-VERIFICATION.md`（482/704、窄屏 overlay、Back 恢复）
- 代码：`src/features/Reviews/ReviewsPage.tsx`、`reviewsSurface.ts`、`ReviewPullRequestPage.tsx`、`src/features/MyWork/WorkQueryResults.tsx`、`src/features/WorkSurface/WorkSurface.tsx`、`src/spa/router/desktopRouter.shared.tsx:766-774`
