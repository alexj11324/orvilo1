name: PR26 Race Validation
on:
  push:
    branches: [fix/task-pr-audit-20260916]
    paths: [.github/workflows/pr26-race-validation.yml]
permissions:
  contents: write
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: |
          git status --short
          git push origin HEAD:fix/task-pr-audit-20260916
