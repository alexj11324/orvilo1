# Project detail — whole-page parity audit — 2026-09-24 (checkpoint→advance)

Re-measured the **live Linear reference** this round (the PD8 slice relied on a
stale row measurement). Reference: `linear.app/bdiverifier` —
`project/orvilo-linear-parity-3eb13143d468` (overview+issues) and cross-checks on
`project/daymark-dayscount-171f62080db4` (has a milestone),
`project/contextfocus-341607183971`, and `my-issues/assigned` (second row sample).
Candidate: this worktree's `dev:spa` on `localhost:9892`,
`ws-useragenttes/project/parity-voyager-launch`. All measurements 1440×900.

## CORRECTED FACT — issue row leading cells

**PD8's premise was wrong.** Live DOM (verified on project issues AND my-issues,
both surfaces identical) — direct flex children of the row link:

```
[checkbox 'Select issue' @261] [PRIORITY icon @287, svg aria='No Priority']
[ID 'ORV-5' @311] [STATUS icon @379-380, circle svg aria-label 'In Progress']
[title @403] [sub-issue '10/14' + labels inside title cluster] [assignee @911]
[date 'Sep 20' @935] [overflow @1003]
```

Order is **priority → ID → status → title**. Both icons are display-only in the
row (`cursor:default`, no button/tabindex — row is one `<a href>` that navigates).
Row pitch **44px** (candidate 38px).

Candidate row post-PD8 (`linearIssueRow`): `[status][ID][title]` — the priority
icon was deleted on a stale measurement, and status sits before the ID. Fix:
restore the priority icon before the ID, move the status icon after the ID, bump
row to \~44px. Keep both as the existing dropdown triggers (same icons, editing
preserved — superset of the reference's display-only glyphs).

## Tabs

Reference has **exactly 3 tabs — Overview | Activity | Issues** (+ a trailing
"Add new view" +), verified on a project WITH a milestone (Daymark: "dsfgsdfg") —
milestones are a body section + rail card, never a tab. Candidate shows a 4th
**Milestones** tab. Fix: drop it from `TabsBar` (the `/milestones` route stays
for deep links).

## Issues toolbar (tab-row right end)

Reference: `Add filter` funnel @1325 · `Display options` sliders @1359 ·
`Close project details` ✕ @1393.
Candidate: `Tasks ▾` collection switcher · `Visibility: All` · `+` create ·
`TasksGroupConfig` (display/grouping) · `Add filter` · `+ New view` · peek toggle.
The Tasks/Scheduled switcher and visibility are Orvilo domain — kept. Filter +
display + new-view equivalents exist. **Gap: no "close rail" affordance** (the
reference's ✕ hides the properties rail; ours is layout-mounted, unclosable).

## Group header

Reference: `[collapse chevron][status icon] name [count] … [+ Create new issue]`
(right end). Candidate: `[chevron][status icon] name [count] [—divider—]`.
Matches except **the per-group "+" is missing** — our inline composer lives at
the list top only and `CreateTaskInlineEntry` takes no `status` prop. Deferred:
needs a group-scoped composer mount, not a chrome tweak.

## Selection / bulk

Reference rows carry a hover `Select issue` checkbox → multi-select + bulk bar.
Candidate has no selection model on this surface. **Gap — documented, deferred**
(large feature; MyWork owns any shared selection state).

## Page header

Reference: `[menu][project name][star][… project actions]` + right `[copy page
URL][notifications bell]`. Candidate: switcher + `Planned` status tag + member
avatars + star. Gaps: no copy-URL / bell / project-actions(…) menu; the status
tag + members cluster is an Orvilo extra (harmless, kept).

## Right rail

Both sides: Properties / Milestones / Progress / Activity cards, persistent
across Overview-Activity-Issues. Reference extras we lack: `Slack` row, `Labels`
row inside Properties (candidate HAS Labels), `Add dependency` button in the
Properties header, Progress chart + Assignees/Labels breakdown, rail close ✕.
Deferred: dependency model and the progress-chart breakdown are data features.

## Overview body

Both: icon + title + description, property summary strip, Resources section +
"Write first project update" composer, Description doc, Milestones section.
Ref milestone rows are compact `icon name · date · issues · %`; ours render
cards with an "Add a description…" affordance — acceptable domain diff.

## Loading / error / empty / responsive

`TaskList` has skeleton + AsyncBoundary error + empty states; rail unmounts under
960px. Row click navigates (matches ref `<a>`); peek pane is opt-in via the
toolbar toggle. Responsive spot-checked (rail hides <960) — no new gap found.

## Acceptance frozen for this advance

- [x] Row leads `[priority icon][ID][workflow status icon][title]`, pitch \~44px
- [x] No Milestones tab; Overview|Activity|Issues only
- [x] My Issues / Team Issues rows unchanged (flag stays opt-in)
- [x] Scoped `bun run check` pass (86 tests and lint); Electron evidence in `runtime-acceptance/pd8b-row-order-tabs/`
- [ ] CI green on pushed head
