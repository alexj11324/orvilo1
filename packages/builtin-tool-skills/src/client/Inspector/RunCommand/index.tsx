'use client';

import { createRunCommandInspector } from '@orvilo/shared-tool-ui/inspectors';

export const RunCommandInspector = createRunCommandInspector(
  'builtins.orvilo-skills.apiName.runCommand',
);
