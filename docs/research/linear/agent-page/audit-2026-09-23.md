# Agent 页 parity 审计（2026-09-23）

对照：Linear `/bdiverifier/agent`（参考端）× `app://renderer/ws-useragenttes/agent/inbox`（候选端）。
Linear issue：ORV-140。

**审计性质**：只读静态审计。本文件不产出新的运行时测量；所有「实测」行引用既有证据，
代码行号在 HEAD `5358ec676` 上核对。inbox landing slice 已在 `a1b1f5e6a`
（`✨ Restore Agent inbox landing and safe short-height scrolling`）提交，是本 HEAD 的祖先；
`git diff a1b1f5e6a..HEAD` 对 `InboxAgentLanding.tsx` / `ConversationArea.tsx` /
`AgentIdSync.tsx` / `src/features/AgentHome/` / `WorkingSidebar/` 均为空（仅 AgentSidebar
Topic 行有两处无关小改）。

证据来源：`ref-sweep.md` §4（状态 A/B）与 §8.5（空态回审修正）、`TRIAGE.md` §3.5（A1–A6）、
`BEHAVIORS.md`、`PAGE_TOPOLOGY.md`、`components/inbox-new-chat.spec.md`、
`VERIFICATION.md`（候选端 Electron 实测，基线 `c5f45c9a`+diff，即 `a1b1f5e6a`）、
`CANDIDATE-DIAGNOSIS.md`、`cand-sweep.md` §4（修复前快照，URL 尚带假 topic 段）。

---

## 1. 差异清单表

判定沿用 TRIAGE 口径：`对齐` / `缺失`（参考端有、候选端无）/ `多出`（候选端有、参考端无）/
`structural-difference` / `partial`（有对应物但形态或位置不同）/ `未验`。

| #   | 区块              | 参考端                                                                                                                  | 候选端现状（源码）                                                                                                                                                                                                                                                         | 判定                                                                         |
| --- | ----------------- | ----------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| D1  | 左导航            | 六页共用 workspace NAV（244px @1440）                                                                                   | NavPanel 外壳共用（280px），内容被 `NavPanelPortal navKey="agent"` 整槽换成 Agent 专属：agent 切换器 + `New Topic`/`Search`/`Profile`/`Tasks` 等 NavItem + Topics 手风琴列表                                                                                               | `structural-difference`（=A6），与进行中的 nav 工作流耦合                    |
| D2  | 页头              | 44px；仅 `New chat ⌄` 触发器（`aria-label="Switch agent chat"`，92x28，12/500，9999r）；窄屏多一个 Menu 按钮            | `NavHeader` 高 44 对齐；内容不同：左 = `Tags` 静态文本 `New Topic`（14/600，非触发器）+ `MoreHorizontal` 菜单；右 = `TerminalPanelToggle` + `TopicCommentButton` + `ShareButton` + `WorkingPanelToggle`（≤2 个 28x28）+ 条件出现的 `OpenInAppButton`/`AgentMigrationBadge` | partial（高对齐，内容 = 多出 + 缺失）→ A3                                    |
| D3  | 会话历史入口      | 页头标题触发器开 320px 菜单（`Chat history` 搜索 + 行）；右下另有 `Chat history` 开同一份数据                           | 页头标题是静态文本，无历史菜单；历史 = 左导航 Topics 列表（`AgentSidebar/Body.tsx:53`）；`MoreHorizontal` 菜单是 topic 操作集                                                                                                                                              | 缺失（触发器 + 底部入口均无对应物）                                          |
| D4  | 右栏              | 无                                                                                                                      | 两块候选端独有：`Overview` 浮板（`overviewWidth 340 + 32`，经 `OverviewSlot` portal 进会话列右缘）+ `RightPanel` 标签面板（skills/documents/web/works/comments/review/files/browser/params）                                                                               | 多出（=A1）                                                                  |
| D5  | 右下工具行        | `Agent`（开全局浮层 quick-chat，带 minimize/full-page/close）+ `Chat history`                                           | 无对应组件（候选端 `Agent` chip 是 composer 内的 agent 切换器，语义不同）                                                                                                                                                                                                  | 缺失；OUTPUT-PLAN 已把全局 quick-chat 划出本 slice                           |
| D6  | 新会话主体        | 状态 A：composer 居中 + 336 插画；状态 B（终态）：composer 上移～91px + 示例区                                          | `InboxAgentLanding`：`ProductLogo` 水印 336（`opacity:.035`、`aria-hidden`、`pointer-events:none`、dark 下 invert）+ composer 组 `margin-block:auto` **单态**居中                                                                                                          | A 态对齐（实测 y400.5 vs 参考 395）；B 态无对应 → A5                         |
| D7  | 示例区            | `Get started with some examples`（12/450）+ `Dismiss`（24x24）+ 3 卡（232x135、8r、pitch 240，文案逐字见 ref-sweep §4） | 不存在。最接近的机制是 `OpeningQuestions`（≤5 个胶囊，`fillInputMessage`），但它 inbox 默认 `[]` 且根本不在 landing 渲染（只在 `AgentHome` 内）                                                                                                                            | 缺失（=A4），六页里单块缺失最大                                              |
| D8  | composer 卡       | 712x104 (A)/712x112 (B)，12px padding、10r、近白面、双层浅影                                                            | 712x106 @1440 实测；宽度链 = landing 包装 `min(744px,100%-16px)` → `WideScreenContainer` `paddingInline:16` → 卡 712                                                                                                                                                       | 几何对齐                                                                     |
| D9  | composer 内控制行 | 卡内底行：`Skills` chip 74x24 + 附件 24x24 + 发送 24x24 (12r)                                                           | 两层不同：① 卡内 `ActionBar` 底行 = 左 `+`（Plus 菜单内含 Skills 项）/ 听写 / 展开，右 agent chip / 语音 /token/ 发送 32px 圆；② 卡外下方 `ControlBar` 兄弟节点 = `Agent`/`No device`/`Manual` chips + credits                                                             | `structural-difference`（参考端 3 件卡内；候选端 5+ 件且控制条在卡外）       |
| D10 | 编辑器与占位      | contenteditable ProseMirror，15/400，676x24；`Ask Linear...` 是 placeholder 非文本节点                                  | `InputEditor` + i18n `sendPlaceholder`=`Ask, create, or start a task, <hotkey>`+`...`；实测可见区 686x56                                                                                                                                                                   | 文案 / 高度不同；两端 placeholder 都不是 innerText（均靠 aria-label / 组件） |
| D11 | populated chat    | 居中阅读列；user bubble 右对齐；`Worked for …` 披露行内联展开；composer 沉底                                            | `ChatList`+`VirtualizedList` 保留；`WorkflowCollapse`（含 `formatReasoningDuration`）有同类披露机制；composer 沉底同                                                                                                                                                       | partial（机制存在，视觉 parity 未验）                                        |
| D12 | 页头 favorite     | populated 页头有 favorite switch                                                                                        | favorite 收在 `MoreHorizontal` 菜单内（`useMenu.tsx:219-226`）                                                                                                                                                                                                             | partial（位置不同）                                                          |
| D13 | chat options      | `Copy as markdown` + `Delete`                                                                                           | `useMenu` 超集（favorite/autoRename/rename/copyCwd/openInPopup/copySessionId/fullWidth/delete），**无 copy-as-markdown 对应项**                                                                                                                                            | partial                                                                      |
| D14 | 欢迎身份卡        | 参考端任何状态都没有头像 + 标题卡                                                                                       | `AgentHome`（64px 头像 + `Orvilo AI` 24/700 + welcome 文案）仍被 `ChatList welcome` 引用 → 非 inbox agent、inbox + 空 topic 仍会渲染                                                                                                                                       | 目标面已消除（landing 不走它）；组件在其他分支仍在                           |
| D15 | 多余空块          | —                                                                                                                       | `AgentHome/index.tsx:21` `<Flexbox flex={1} />`（441px 零子节点空块的来源）仍在                                                                                                                                                                                            | 目标面已绕过；组件级缺陷遗留（=A2）                                          |
| D16 | 响应式            | 768/390：NAV 隐藏 + 页头 Menu 按钮；composer 712/342                                                                    | 390x844 实测：composer 332x106、水印 326、无横向裁剪；768 未采                                                                                                                                                                                                             | partial                                                                      |
| D17 | 页头模式          | 始终 44px 实心条                                                                                                        | 列宽 ≥1200px 时 header 变浮动透明（`FLOATING_HEADER_QUERY`，Header/index.tsx:28-52）                                                                                                                                                                                       | 多出行为（参考端未观测到）                                                   |
| D18 | 挂载辅助件        | —                                                                                                                       | `ToolAuthAlert`（landing 内）、`ComposerDraftReceiver`/`ThreadHydration`/`ChatMiniMap`/`MessageFromUrl` 等                                                                                                                                                                 | 多出但属 spec 明示保留的 Orvilo 能力，不计为缺陷                             |

## 2. A1–A6 现状判定

| #   | TRIAGE 原判定                                               | 现状                                     | 依据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ----------------------------------------------------------- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | 右栏 372x779 只渲染 `Skills`/`Documents` 两行，\~715px 空白 | **结构仍在、症状可能已间接缓解，需复测** | 372 = `overviewWidth(340)+32`，即 `Overview` 浮板而非 RightPanel。当年只剩两行的原因链：inbox 非 local/device 执行 → `environmentAvailable=false` → workspace Empty（Overview/index.tsx:452）不渲染；假 topic 段 `ws-useragenttes` 使 `activeTopicId` 非空 → `!topicId` 失败 → 「Ready to work」Empty（:487-494）被抑制；`ProgressSection` total=0 → null（ProgressSection/index.tsx:154）。路由修复后 `topicId=null`，Empty 理论上会补上 —— 但浮板本身的存在性（多出）不变。代码自 cand-sweep 起未动 |
| A2  | `AgentHome/index.tsx:21` 441px 零子节点空块                 | **目标面已闭合，组件缺陷遗留**           | inbox + 无 topic 走 `InboxAgentLanding`（`ConversationArea.tsx:162-166`），不再渲染 AgentHome；但 `:21` 的 `<Flexbox flex={1} />` 仍在，`ChatList welcome` 路径（非 inbox agent、inbox + 空 topic）仍会渲染出同样的推底空块                                                                                                                                                                                                                                                                           |
| A3  | 页头 5 个 28x28 图标按钮密集排布                            | **仍在，未动**                           | `Header/index.tsx:186-189`：`TerminalPanelToggle`+`TopicCommentButton`+`ShareButton`+`WorkingPanelToggle`（其内部最多再出 2 枚，:42-65），左槽另有 `MoreHorizontal`（HeaderActions）+ 条件 `OpenInAppButton`/`AgentMigrationBadge`。参考端同区域只有标题触发器                                                                                                                                                                                                                                        |
| A4  | 示例区（标题 + 3 卡 + Dismiss）候选端完全不存在             | **仍缺**                                 | 全仓无对应组件；`OpeningQuestions` 是不同机制（agent-config 驱动、填 input、胶囊形态）且不在 landing 渲染。注意参考端侧该态的触发 / 持久化契约也未验（见 §4-2）                                                                                                                                                                                                                                                                                                                                       |
| A5  | composer 两态位移～91px，固定居中必有一态错                 | **仍未实现 / 未验**                      | landing 是 `margin-block:auto` 单态居中（`InboxAgentLanding.tsx:20`）。参考端 B 态依赖示例区存在 —— A4 不落，A5 无可挂载物；若日后加示例区，放进同一 composer 组即可让整组自然上移，不必写死 91px                                                                                                                                                                                                                                                                                                     |
| A6  | 候选端换了 Agent 专属左导航                                 | **仍在**                                 | `AgentSidebar/index.tsx:7` `NavPanelPortal navKey="agent"`：NavPanel 外壳共用、内容专属（切换器 + Nav+Topics 列表）。TRIAGE §7 标注与进行中 nav 工作流（PAGE-INVENTORY B/C 组）耦合，不宜在本 slice 内单独动                                                                                                                                                                                                                                                                                          |

**附带：修复前 cand-sweep 的 URL 异常（`agent/agt_*/ws-useragenttes` 假 topic 段）已修复** —
`AgentIdSync.tsx:10-18` 的 `getAgentRouteSuffix` 改为从真实 `/agent/:aid` 边界切子路径，
回归测试 `AgentIdSync.test.tsx:115-138` 锁定 workspace 前缀不再泄漏为 topic 段
（VERIFICATION.md 红 / 绿双证）。

## 3. 状态覆盖清单

参考端已观测状态 → 候选端对应物与证据等级：

| 状态                          | 参考端证据                                       | 候选端实现                                                                                   | 覆盖                                                                                   |
| ----------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| 新会话 A（composer 单卡居中） | ref-sweep §4 状态 A                              | `InboxAgentLanding`，实测 712x106\@y400.5、水印 336、390px 无裁剪                            | **已覆盖**（VERIFICATION.md，SHA `a1b1f5e6a`，截图 `/tmp/orvilo-agent-new-chat*.png`） |
| 新会话 B（+ 示例区终态）      | ref-sweep §4 状态 B（435 字符、3 卡、`Dismiss`） | 无对应物                                                                                     | **缺失**（A4+A5）                                                                      |
| populated chat                | PAGE\_TOPOLOGY「Populated chat」                 | `ChatList` 路径保留；topic 点击 / 返回已实测；消息卡与 Work 披露视觉未验                     | partial                                                                                |
| 标题历史菜单                  | BEHAVIORS.md：320px、`Chat history` 搜索、行导航 | 无；历史在左导航 Topics                                                                      | 缺失                                                                                   |
| 底部 `Chat history`           | BEHAVIORS.md：同数据不同锚点                     | 无                                                                                           | 缺失                                                                                   |
| `Skills` 菜单                 | 可搜索菜单 + `Create skill`（未点 create）       | composer 无 Skills chip；`+` 菜单内有 Skills 项；WorkingSidebar 有 skills tab                | partial（入口形态不同）                                                                |
| `Chat options`                | `Copy as markdown` + `Delete`                    | `MoreHorizontal` 菜单超集含 delete；**无 copy-as-markdown**                                  | partial                                                                                |
| `Worked for …` 展开           | 内联展开、保持滚动位置                           | `WorkflowCollapse` 同类机制存在                                                              | partial（视觉未验）                                                                    |
| 底部 `Agent` 全局 quick-chat  | 浮层 + minimize/full-page/close                  | 无                                                                                           | 缺失（OUTPUT-PLAN 划出 slice）                                                         |
| 768 / 390 窄屏                | NAV 隐藏 + Menu 按钮                             | 390 实测过（composer 332）；768 未采；候选端 NAV 隐藏行为未记录                              | partial                                                                                |
| loading / error               | 未观测                                           | `ChatList` skeleton / first-load `AsyncError` / 背景 `RefreshError`                          | 候选端有实现，参考端无对照                                                             |
| 权限 / 只读                   | 未观测                                           | `useResourceAccess` gating、`ReadOnlyAgentHome`、subagent 只读 hint、`topicPending` 迁移占位 | 候选端多出能力（spec 明示保留）                                                        |
| `ToolAuthAlert` 授权行        | 无对应概念                                       | landing 内挂载；overflow 滚动边界经 DOM 探针验证                                             | 多出但刻意保留；真实 OAuth 行样式未验                                                  |

## 4. 待运行时验证项

按依赖排序；标 `[需授权]` 的涉及参考端副作用或产品决定，不能擅自动：

1. **A1 复测**：修正后路由（无假 topic 段）下，inbox 无 topic 时 `Overview` 浮板是否补出
   「Ready to work / Workspace activity and outputs…」Empty（`Overview/index.tsx:487-494` 的
   `!environmentAvailable && !topicId` 分支），以及浮板在 landing 右缘的实际位置
   （`OverviewSlot.Outlet` 在会话列内，`(chat)/_layout/index.tsx:60`）。
2. **A4/A5 前置**：参考端 `Dismiss` 是否持久化、示例区的触发 / 召回契约（账号级 flag？延迟注水？）
   —— 需要一次参考端点击（有副作用，`[需授权]`）。contract 不定，候选端实现无可对照。
3. **populated chat 视觉 parity**：user bubble 右对齐、`Worked for` 披露行样式、页头
   favorite/options —— 候选端 `ChatList`/`WorkflowCollapse` 从未做过视觉对照。
4. **菜单面产品决定** `[需授权]`：标题 `Switch agent chat` 触发器菜单、右下 `Agent`/`Chat history`
   是否要做（OUTPUT-PLAN 已划出本 slice；若做，候选端历史数据源 = 左导航 Topics 同一 store）。
5. **768px 断点**：候选端 NAV 是否隐藏、页头是否出现 Menu 对应物（`ToggleLeftPanelButton`）。
6. **hover/focus/ 激活态**：两端都未采（BEHAVIORS.md 明示 open gap）。
7. **`ToolAuthAlert` 真实样式**：此前只验证 DOM fixture 的滚动边界与 multiline 可达性，
   真实 connector/plugin 授权行的样式与 OAuth 动作未验。
8. **placeholder 渲染机制对照**：参考端 `Ask Linear...` 的具体元素未定位（非 `::before`）；
   候选端是 `InputEditor/Placeholder.tsx` 组件（含 hotkey 文案）。采 aria-label/title 属性对照。
9. **页头浮动模式 ≥1200px**：列宽达标时候选端页头变浮动透明（`FLOATING_HEADER_QUERY`），
   参考端未观测到此行为 —— 需要两张截图对照后决定保留与否。
10. **编辑器初始高度差**：候选端可见编辑区 686x56 vs 参考端 676x24（差 32px，疑似
    `ChatInput minHeight 36`/ 初始行高不同），影响 composer 卡 104↔112 的两态高度对齐判断。

---

## 附：本 slice 已交付与未交付的边界（摘自 OUTPUT-PLAN，供阅读对照）

- 已交付：workspace 前缀路由修复 + inbox 无 topic 时的居中 landing（含短高滚动兜底）。
- 未交付（本文件逐条登记为缺口）：示例区（A4）、composer 两态（A5）、历史 / 选项菜单的
  参考端形态、右下 quick-chat、populated-chat 视觉 parity、A1/A3/A6 的「多出」面收敛。
- TRIAGE 排序理由仍成立：本页差异绝对值最大但最不可 join —— 多出项（专属导航、
  WorkingSidebar、控制条）在参考端无可 join 位置，关掉它们是产品决定而非元素编辑。
