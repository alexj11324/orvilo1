# Issue detail and workflow marks: Electron acceptance

Product source commit: `13f4e5716` (stacked on Team Home PR #226 at `a6005a036`).

Runtime: Electron development build from this worktree, copied local test profile, dark appearance, en-US, 1440 × 900 CSS pixels, DPR 2. The Electron renderer used the existing local backend on port 3010. Screenshots were captured with CDP port 9233 after the page had populated; clicks used CDP hit testing.

| Surface                  | Observed result                                                                                                                                                                                                                                            | Evidence                                                  |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| Team Home → PMI-4 detail | The Recent issues row linked to `/ws-useragenttes/task/PMI-4/blocking-verify-downstream-acceptance`; a real click opened the populated detail without “Failed to load”.                                                                                    | [Detail](./electron-detail-1440.png)                      |
| Issue detail             | The title input computed to 24px / 600 / 38.4px and began at x=313px. Properties began at x=993px. The business state “In progress” appeared before the separately labeled “Execution: Backlog”; the parent PMI-3 used `data-workflow-icon="in_progress"`. | [Detail](./electron-detail-1440.png)                      |
| Project Issues list      | The Done group heading and issue row both used `data-workflow-icon="done"`.                                                                                                                                                                                | [Project Issues](./electron-project-issues-list-1440.png) |
| Team Triage              | Four populated rows used `data-workflow-icon="triage"`; their links used issue identifiers such as `/task/PARITY-1/...`.                                                                                                                                   | [Triage](./electron-team-triage-1440.png)                 |

The populated Linear issue reference was ORV-135 at 1440 × 900: its title began at x=306px and its Properties rail at x=1001px. Orvilo PMI-4 has different text and task data, so the measurements check page geometry only. The screenshots do not prove a full page match: action placement, all property interactions, and populated content parity remain separate work.

The service now includes provider workflow state on readable dependency and subtask DTOs. Focused server and UI regression tests passed, including fallback to execution marks and redaction of unavailable dependencies. The Electron run above was connected to an older backend checkout, so it **does not** verify the new dependency/subtask fields through the full client–server path. A separate local backend attempt could not start from this temporary worktree because its symlinked dependencies failed in Next/Turbopack and native package install. This PR remains draft until that runtime path and the current remote CI are checked.

The Linear Project Overview reference and Orvilo Project Overview both have no “Recent tasks” block. That label belongs to global Home; Team Home has “Recent issues” (translated as “最近的任务” in zh-CN). Project Overview status is the project lifecycle, which uses a different mark from issue workflow states in the Linear reference.
